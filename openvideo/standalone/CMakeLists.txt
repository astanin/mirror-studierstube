ADD_EXECUTABLE(openvideo-bin main.cxx)
INCLUDE_DIRECTORIES(include ${ACE_INCLUDE_DIR})
TARGET_LINK_LIBRARIES(openvideo-bin openvideo)
INSTALL(TARGETS openvideo-bin DESTINATION ${CMAKE_INSTALL_BINDIR})
